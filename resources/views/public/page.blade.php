<!doctype html>
<html lang="{{ $page['locale'] }}"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1"><title>{{ $title }}</title>@if($description)<meta name="description" content="{{ $description }}">@endif @if($canonicalUrl)<link rel="canonical" href="{{ $canonicalUrl }}">@endif @if($robots)<meta name="robots" content="{{ $robots }}">@endif</head>
<body>@if($navigation)<nav aria-label="Site navigation"><ul>@include('larena-content::public.partials.navigation-items', ['items' => $navigation])</ul></nav>@endif<main><article><header><p>{{ $page['safe_type_metadata']['label'] ?? $page['type_key'] }}</p><h1>{{ $heading }}</h1></header>@foreach($fields as $field)<section><h2>{{ $field['label'] }}</h2>@if($field['type'] === 'file' && $field['url'])<figure><img src="{{ $field['url'] }}" alt="{{ $field['alt'] }}"><figcaption>{{ $field['value'] }}</figcaption></figure>@elseif($field['type'] === 'relation' && $field['url'])<p><a href="{{ $field['url'] }}">{{ $field['value'] }}</a></p>@else<p>@if(is_bool($field['value'])){{ $field['value'] ? 'Yes' : 'No' }}@else{{ $field['value'] }}@endif</p>@endif</section>@endforeach</article></main></body></html>
